# Windows scripts MUST keep CRLF line endings.
# cmd.exe misbehaves (breaks if/else blocks, ^ continuations) with LF.
# This forces CRLF for every clone/checkout on every OS.
*.bat text eol=crlf
*.cmd text eol=crlf
*.vbs text eol=crlf
*.ps1 text eol=crlf
